'use strict'

expect = require('chai').expect

describe 'Test', ->
  it 'should attach a list of awesomeThings to the scope', ->
    # server = require('../../api/app')
    # expect(server.getThing()).to.have.length(4);

